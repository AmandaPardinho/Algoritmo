Algoritmo "Salto"

Var

contador, valor, salto: inteiro

Inicio

contador <- 0
escreva("Até quanto deseja contar? ")
leia(valor)

escreva("Qual o valor do salto do contador? ")
leia(salto)

enquanto (contador <= valor) faca
   escreval(contador)
   contador <- contador + salto
fimenquanto

Fimalgoritmo