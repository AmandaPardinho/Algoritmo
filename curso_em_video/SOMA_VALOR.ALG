Algoritmo "Soma Valor"

Var

contador, numero, soma: inteiro

Inicio

contador <- 1
soma <- 0

enquanto (contador <= 5) faca
   escreval("Digite um número: ")
   leia(numero)
   
   soma <- soma + numero
   
   contador <- contador + 1
fimenquanto

escreval("A soma de todos os valores foi ", soma)

Fimalgoritmo